# PyMuPDF Lambda Layer

This folder contains the build input for the compliance PDF parser layer.

- `requirements.txt`: pinned Python dependencies for the layer.
- `python/`: generated by `pnpm run lambda:build:pymupdf-layer` (ignored in git).

Build and deploy in one command:

```bash
pnpm run infra:deploy:compliance
```
