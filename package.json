{
  "name": "crop-copilot",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "infra:build": "pnpm --filter infra build",
    "infra:synth": "pnpm --filter infra synth",
    "infra:diff": "pnpm --filter infra diff",
    "infra:bootstrap": "pnpm --filter infra bootstrap",
    "infra:deploy": "pnpm --filter infra deploy"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.10"
  }
}
